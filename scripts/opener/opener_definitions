# Everything before the first space on each line is treated as the file extension
# or mimetype. Everything after the first space is the command to run the file with
# that extension/mime.

.txt subl3 -n
.mp4 mpv
inode/x-empty subl3
application/pdf zathura
